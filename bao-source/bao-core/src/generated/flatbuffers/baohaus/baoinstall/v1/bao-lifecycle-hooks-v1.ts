// automatically generated by the FlatBuffers compiler, do not modify
import { Builder } from '@baohaus/flatbuf-bao/builder';
import type { IGeneratedObject, Offset } from '@baohaus/flatbuf-bao/builder';
import { SIZE_PREFIX_LENGTH } from '@baohaus/flatbuf-bao/constants';
import { ByteBuffer, Encoding } from '@baohaus/flatbuf-bao/reader';
import type { IUnpackableObject } from '@baohaus/flatbuf-bao/reader';
/**
 * Lifecycle hook declarations for pre/post install/uninstall phases.
 */
export class BaoLifecycleHooksV1 implements IUnpackableObject<BaoLifecycleHooksV1T> {
  bb: ByteBuffer | null = null;
  bb_pos = 0;
  __init(i: number, bb: ByteBuffer): BaoLifecycleHooksV1 {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsBaoLifecycleHooksV1(
    bb: ByteBuffer,
    obj?: BaoLifecycleHooksV1,
  ): BaoLifecycleHooksV1 {
    return (obj || new BaoLifecycleHooksV1()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  static getSizePrefixedRootAsBaoLifecycleHooksV1(
    bb: ByteBuffer,
    obj?: BaoLifecycleHooksV1,
  ): BaoLifecycleHooksV1 {
    bb.setPosition(bb.position() + SIZE_PREFIX_LENGTH);
    return (obj || new BaoLifecycleHooksV1()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  preInstall(): string | null;
  preInstall(optionalEncoding: Encoding): string | Uint8Array | null;
  preInstall(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  postInstall(): string | null;
  postInstall(optionalEncoding: Encoding): string | Uint8Array | null;
  postInstall(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  preUninstall(): string | null;
  preUninstall(optionalEncoding: Encoding): string | Uint8Array | null;
  preUninstall(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  postUninstall(): string | null;
  postUninstall(optionalEncoding: Encoding): string | Uint8Array | null;
  postUninstall(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 10);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  healthCheck(): string | null;
  healthCheck(optionalEncoding: Encoding): string | Uint8Array | null;
  healthCheck(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 12);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  static startBaoLifecycleHooksV1(builder: Builder) {
    builder.startObject(5);
  }

  static addPreInstall(builder: Builder, preInstallOffset: Offset) {
    builder.addFieldOffset(0, preInstallOffset, 0);
  }

  static addPostInstall(builder: Builder, postInstallOffset: Offset) {
    builder.addFieldOffset(1, postInstallOffset, 0);
  }

  static addPreUninstall(builder: Builder, preUninstallOffset: Offset) {
    builder.addFieldOffset(2, preUninstallOffset, 0);
  }

  static addPostUninstall(builder: Builder, postUninstallOffset: Offset) {
    builder.addFieldOffset(3, postUninstallOffset, 0);
  }

  static addHealthCheck(builder: Builder, healthCheckOffset: Offset) {
    builder.addFieldOffset(4, healthCheckOffset, 0);
  }

  static endBaoLifecycleHooksV1(builder: Builder): Offset {
    const offset = builder.endObject();
    return offset;
  }

  static createBaoLifecycleHooksV1(
    builder: Builder,
    preInstallOffset: Offset,
    postInstallOffset: Offset,
    preUninstallOffset: Offset,
    postUninstallOffset: Offset,
    healthCheckOffset: Offset,
  ): Offset {
    BaoLifecycleHooksV1.startBaoLifecycleHooksV1(builder);
    BaoLifecycleHooksV1.addPreInstall(builder, preInstallOffset);
    BaoLifecycleHooksV1.addPostInstall(builder, postInstallOffset);
    BaoLifecycleHooksV1.addPreUninstall(builder, preUninstallOffset);
    BaoLifecycleHooksV1.addPostUninstall(builder, postUninstallOffset);
    BaoLifecycleHooksV1.addHealthCheck(builder, healthCheckOffset);
    return BaoLifecycleHooksV1.endBaoLifecycleHooksV1(builder);
  }

  unpack(): BaoLifecycleHooksV1T {
    return new BaoLifecycleHooksV1T(
      this.preInstall(),
      this.postInstall(),
      this.preUninstall(),
      this.postUninstall(),
      this.healthCheck(),
    );
  }

  unpackTo(_o: BaoLifecycleHooksV1T): void {
    _o.preInstall = this.preInstall();
    _o.postInstall = this.postInstall();
    _o.preUninstall = this.preUninstall();
    _o.postUninstall = this.postUninstall();
    _o.healthCheck = this.healthCheck();
  }
}

export class BaoLifecycleHooksV1T implements IGeneratedObject {
  constructor(
    public preInstall: string | Uint8Array | null = null,
    public postInstall: string | Uint8Array | null = null,
    public preUninstall: string | Uint8Array | null = null,
    public postUninstall: string | Uint8Array | null = null,
    public healthCheck: string | Uint8Array | null = null,
  ) {}

  pack(builder: Builder): Offset {
    const preInstall = this.preInstall !== null ? builder.createString(this.preInstall!) : 0;
    const postInstall = this.postInstall !== null ? builder.createString(this.postInstall!) : 0;
    const preUninstall = this.preUninstall !== null ? builder.createString(this.preUninstall!) : 0;
    const postUninstall =
      this.postUninstall !== null ? builder.createString(this.postUninstall!) : 0;
    const healthCheck = this.healthCheck !== null ? builder.createString(this.healthCheck!) : 0;

    return BaoLifecycleHooksV1.createBaoLifecycleHooksV1(
      builder,
      preInstall,
      postInstall,
      preUninstall,
      postUninstall,
      healthCheck,
    );
  }
}
