// automatically generated by the FlatBuffers compiler, do not modify
import { Builder } from '@baohaus/flatbuf-bao/builder';
import type { IGeneratedObject, Offset } from '@baohaus/flatbuf-bao/builder';
import { SIZE_PREFIX_LENGTH } from '@baohaus/flatbuf-bao/constants';
import { ByteBuffer, Encoding } from '@baohaus/flatbuf-bao/reader';
import type { IUnpackableObject } from '@baohaus/flatbuf-bao/reader';
export class BaoInstallEventV1 implements IUnpackableObject<BaoInstallEventV1T> {
  bb: ByteBuffer | null = null;
  bb_pos = 0;
  __init(i: number, bb: ByteBuffer): BaoInstallEventV1 {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsBaoInstallEventV1(
    bb: ByteBuffer,
    obj?: BaoInstallEventV1,
  ): BaoInstallEventV1 {
    return (obj || new BaoInstallEventV1()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  static getSizePrefixedRootAsBaoInstallEventV1(
    bb: ByteBuffer,
    obj?: BaoInstallEventV1,
  ): BaoInstallEventV1 {
    bb.setPosition(bb.position() + SIZE_PREFIX_LENGTH);
    return (obj || new BaoInstallEventV1()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  runId(): string | null;
  runId(optionalEncoding: Encoding): string | Uint8Array | null;
  runId(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  phase(): string | null;
  phase(optionalEncoding: Encoding): string | Uint8Array | null;
  phase(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  manifestName(): string | null;
  manifestName(optionalEncoding: Encoding): string | Uint8Array | null;
  manifestName(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  manifestVersion(): string | null;
  manifestVersion(optionalEncoding: Encoding): string | Uint8Array | null;
  manifestVersion(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 10);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  targetCount(): number {
    const offset = this.bb!.__offset(this.bb_pos, 12);
    return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
  }

  durationMs(): number {
    const offset = this.bb!.__offset(this.bb_pos, 14);
    return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
  }

  message(): string | null;
  message(optionalEncoding: Encoding): string | Uint8Array | null;
  message(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 16);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  static startBaoInstallEventV1(builder: Builder) {
    builder.startObject(7);
  }

  static addRunId(builder: Builder, runIdOffset: Offset) {
    builder.addFieldOffset(0, runIdOffset, 0);
  }

  static addPhase(builder: Builder, phaseOffset: Offset) {
    builder.addFieldOffset(1, phaseOffset, 0);
  }

  static addManifestName(builder: Builder, manifestNameOffset: Offset) {
    builder.addFieldOffset(2, manifestNameOffset, 0);
  }

  static addManifestVersion(
    builder: Builder,
    manifestVersionOffset: Offset,
  ) {
    builder.addFieldOffset(3, manifestVersionOffset, 0);
  }

  static addTargetCount(builder: Builder, targetCount: number) {
    builder.addFieldInt32(4, targetCount, 0);
  }

  static addDurationMs(builder: Builder, durationMs: number) {
    builder.addFieldInt32(5, durationMs, 0);
  }

  static addMessage(builder: Builder, messageOffset: Offset) {
    builder.addFieldOffset(6, messageOffset, 0);
  }

  static endBaoInstallEventV1(builder: Builder): Offset {
    const offset = builder.endObject();
    return offset;
  }

  static createBaoInstallEventV1(
    builder: Builder,
    runIdOffset: Offset,
    phaseOffset: Offset,
    manifestNameOffset: Offset,
    manifestVersionOffset: Offset,
    targetCount: number,
    durationMs: number,
    messageOffset: Offset,
  ): Offset {
    BaoInstallEventV1.startBaoInstallEventV1(builder);
    BaoInstallEventV1.addRunId(builder, runIdOffset);
    BaoInstallEventV1.addPhase(builder, phaseOffset);
    BaoInstallEventV1.addManifestName(builder, manifestNameOffset);
    BaoInstallEventV1.addManifestVersion(builder, manifestVersionOffset);
    BaoInstallEventV1.addTargetCount(builder, targetCount);
    BaoInstallEventV1.addDurationMs(builder, durationMs);
    BaoInstallEventV1.addMessage(builder, messageOffset);
    return BaoInstallEventV1.endBaoInstallEventV1(builder);
  }

  unpack(): BaoInstallEventV1T {
    return new BaoInstallEventV1T(
      this.runId(),
      this.phase(),
      this.manifestName(),
      this.manifestVersion(),
      this.targetCount(),
      this.durationMs(),
      this.message(),
    );
  }

  unpackTo(_o: BaoInstallEventV1T): void {
    _o.runId = this.runId();
    _o.phase = this.phase();
    _o.manifestName = this.manifestName();
    _o.manifestVersion = this.manifestVersion();
    _o.targetCount = this.targetCount();
    _o.durationMs = this.durationMs();
    _o.message = this.message();
  }
}

export class BaoInstallEventV1T implements IGeneratedObject {
  constructor(
    public runId: string | Uint8Array | null = null,
    public phase: string | Uint8Array | null = null,
    public manifestName: string | Uint8Array | null = null,
    public manifestVersion: string | Uint8Array | null = null,
    public targetCount: number = 0,
    public durationMs: number = 0,
    public message: string | Uint8Array | null = null,
  ) {}

  pack(builder: Builder): Offset {
    const runId = this.runId !== null ? builder.createString(this.runId!) : 0;
    const phase = this.phase !== null ? builder.createString(this.phase!) : 0;
    const manifestName = this.manifestName !== null ? builder.createString(this.manifestName!) : 0;
    const manifestVersion =
      this.manifestVersion !== null ? builder.createString(this.manifestVersion!) : 0;
    const message = this.message !== null ? builder.createString(this.message!) : 0;

    return BaoInstallEventV1.createBaoInstallEventV1(
      builder,
      runId,
      phase,
      manifestName,
      manifestVersion,
      this.targetCount,
      this.durationMs,
      message,
    );
  }
}
