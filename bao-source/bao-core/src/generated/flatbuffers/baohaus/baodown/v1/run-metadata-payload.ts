// automatically generated by the FlatBuffers compiler, do not modify
import { Builder } from '@baohaus/flatbuf-bao/builder';
import type { IGeneratedObject, Offset } from '@baohaus/flatbuf-bao/builder';
import { SIZE_PREFIX_LENGTH } from '@baohaus/flatbuf-bao/constants';
import { ByteBuffer, Encoding } from '@baohaus/flatbuf-bao/reader';
import type { IUnpackableObject } from '@baohaus/flatbuf-bao/reader';
/**
 * Run-level metadata for RUN_STARTED, RUN_COMPLETED, RUN_FAILED, RUN_CANCELLED.
 */
export class RunMetadataPayload implements IUnpackableObject<RunMetadataPayloadT> {
  bb: ByteBuffer | null = null;
  bb_pos = 0;
  __init(i: number, bb: ByteBuffer): RunMetadataPayload {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsRunMetadataPayload(
    bb: ByteBuffer,
    obj?: RunMetadataPayload,
  ): RunMetadataPayload {
    return (obj || new RunMetadataPayload()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  static getSizePrefixedRootAsRunMetadataPayload(
    bb: ByteBuffer,
    obj?: RunMetadataPayload,
  ): RunMetadataPayload {
    bb.setPosition(bb.position() + SIZE_PREFIX_LENGTH);
    return (obj || new RunMetadataPayload()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  code(): string | null;
  code(optionalEncoding: Encoding): string | Uint8Array | null;
  code(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  message(): string | null;
  message(optionalEncoding: Encoding): string | Uint8Array | null;
  message(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  detailsJson(): string | null;
  detailsJson(optionalEncoding: Encoding): string | Uint8Array | null;
  detailsJson(optionalEncoding?: any): string | Uint8Array | null {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  static startRunMetadataPayload(builder: Builder) {
    builder.startObject(3);
  }

  static addCode(builder: Builder, codeOffset: Offset) {
    builder.addFieldOffset(0, codeOffset, 0);
  }

  static addMessage(builder: Builder, messageOffset: Offset) {
    builder.addFieldOffset(1, messageOffset, 0);
  }

  static addDetailsJson(builder: Builder, detailsJsonOffset: Offset) {
    builder.addFieldOffset(2, detailsJsonOffset, 0);
  }

  static endRunMetadataPayload(builder: Builder): Offset {
    const offset = builder.endObject();
    return offset;
  }

  static createRunMetadataPayload(
    builder: Builder,
    codeOffset: Offset,
    messageOffset: Offset,
    detailsJsonOffset: Offset,
  ): Offset {
    RunMetadataPayload.startRunMetadataPayload(builder);
    RunMetadataPayload.addCode(builder, codeOffset);
    RunMetadataPayload.addMessage(builder, messageOffset);
    RunMetadataPayload.addDetailsJson(builder, detailsJsonOffset);
    return RunMetadataPayload.endRunMetadataPayload(builder);
  }

  unpack(): RunMetadataPayloadT {
    return new RunMetadataPayloadT(this.code(), this.message(), this.detailsJson());
  }

  unpackTo(_o: RunMetadataPayloadT): void {
    _o.code = this.code();
    _o.message = this.message();
    _o.detailsJson = this.detailsJson();
  }
}

export class RunMetadataPayloadT implements IGeneratedObject {
  constructor(
    public code: string | Uint8Array | null = null,
    public message: string | Uint8Array | null = null,
    public detailsJson: string | Uint8Array | null = null,
  ) {}

  pack(builder: Builder): Offset {
    const code = this.code !== null ? builder.createString(this.code!) : 0;
    const message = this.message !== null ? builder.createString(this.message!) : 0;
    const detailsJson = this.detailsJson !== null ? builder.createString(this.detailsJson!) : 0;

    return RunMetadataPayload.createRunMetadataPayload(builder, code, message, detailsJson);
  }
}
