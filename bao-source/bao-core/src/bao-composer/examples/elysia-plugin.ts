/**
 * .bao Composer Elysia plugin example extension.
 *
 * Provides a no-op extension list so recipe validation proves the module is
 * importable without mutating the host application.
 *
 * @packageDocumentation
 */

const extensions: readonly unknown[] = [];

export { extensions };
export default extensions;
