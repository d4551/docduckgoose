// automatically generated by the FlatBuffers compiler, do not modify
import { Builder } from '@baohaus/flatbuf-bao/builder';
import type { IGeneratedObject, Offset } from '@baohaus/flatbuf-bao/builder';
import { SIZE_PREFIX_LENGTH } from '@baohaus/flatbuf-bao/constants';
import { ByteBuffer } from '@baohaus/flatbuf-bao/reader';
import type { IUnpackableObject } from '@baohaus/flatbuf-bao/reader';
export class PointCloudV1 implements IUnpackableObject<PointCloudV1T> {
  bb: ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:ByteBuffer):PointCloudV1 {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPointCloudV1(bb:ByteBuffer, obj?:PointCloudV1):PointCloudV1 {
  return (obj || new PointCloudV1()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPointCloudV1(bb:ByteBuffer, obj?:PointCloudV1):PointCloudV1 {
  bb.setPosition(bb.position() + SIZE_PREFIX_LENGTH);
  return (obj || new PointCloudV1()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:ByteBuffer):boolean {
  return bb.__has_identifier('PCV1');
}

vertexCount():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

points(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

pointsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

pointsArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startPointCloudV1(builder:Builder) {
  builder.startObject(2);
}

static addVertexCount(builder:Builder, vertexCount:number) {
  builder.addFieldInt32(0, vertexCount, 0);
}

static addPoints(builder:Builder, pointsOffset:Offset) {
  builder.addFieldOffset(1, pointsOffset, 0);
}

static createPointsVector(builder:Builder, data:number[]|Float32Array):Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createPointsVector(builder:Builder, data:number[]|Uint8Array):Offset;
static createPointsVector(builder:Builder, data:number[]|Float32Array|Uint8Array):Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startPointsVector(builder:Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endPointCloudV1(builder:Builder):Offset {
  const offset = builder.endObject();
  return offset;
}

static finishPointCloudV1Buffer(builder:Builder, offset:Offset) {
  builder.finish(offset, 'PCV1');
}

static finishSizePrefixedPointCloudV1Buffer(builder:Builder, offset:Offset) {
  builder.finish(offset, 'PCV1', true);
}

static createPointCloudV1(builder:Builder, vertexCount:number, pointsOffset:Offset):Offset {
  PointCloudV1.startPointCloudV1(builder);
  PointCloudV1.addVertexCount(builder, vertexCount);
  PointCloudV1.addPoints(builder, pointsOffset);
  return PointCloudV1.endPointCloudV1(builder);
}

unpack(): PointCloudV1T {
  return new PointCloudV1T(
    this.vertexCount(),
    this.bb!.createScalarList<number>(this.points.bind(this), this.pointsLength())
  );
}


unpackTo(_o: PointCloudV1T): void {
  _o.vertexCount = this.vertexCount();
  _o.points = this.bb!.createScalarList<number>(this.points.bind(this), this.pointsLength());
}
}

export class PointCloudV1T implements IGeneratedObject {
constructor(
  public vertexCount: number = 0,
  public points: (number)[] = []
){}


pack(builder:Builder): Offset {
  const points = PointCloudV1.createPointsVector(builder, this.points);

  return PointCloudV1.createPointCloudV1(builder,
    this.vertexCount,
    points
  );
}
}
