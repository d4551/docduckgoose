/**
 * @baohaus/htmx-vendor-bao/runtime
 *
 * Bundler entry for the pinned HTMX browser runtime. Apps import this module
 * instead of `htmx.org` directly.
 */

import htmxRuntime from "htmx.org";

export default htmxRuntime;
