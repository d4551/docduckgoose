/**
 * Capability ownership constants.
 *
 * Shared constants for ownership-map caching and alignment surfaces.
 *
 * @shared/constants/capability-ownership
 */

/**
 * Global symbol key used for caching capability ownership snapshots.
 */
export const CAPABILITY_OWNERSHIP_CACHE_SYMBOL_KEY = "vaultmania.capabilityOwnershipCache";

/**
 * Capability ownership contract version identifier.
 */
export const CAPABILITY_OWNERSHIP_CONTRACT_VERSION = "1.0.0";

/**
 * Contract name for the capability ownership map endpoint.
 */
export const CAPABILITY_OWNERSHIP_MAP_CONTRACT_NAME = "capability-ownership-map";

/**
 * Contract name for the capability ownership focus endpoint.
 */
export const CAPABILITY_OWNERSHIP_FOCUS_CONTRACT_NAME = "capability-ownership-focus";

/**
 * Contract name for the capability ownership coverage endpoint.
 */
export const CAPABILITY_OWNERSHIP_COVERAGE_CONTRACT_NAME = "capability-ownership-coverage";

/**
 * Contract name for the capability ownership refresh endpoint.
 */
export const CAPABILITY_OWNERSHIP_REFRESH_CONTRACT_NAME = "capability-ownership-refresh";
